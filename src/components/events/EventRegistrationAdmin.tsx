
// This file is now obsolete, as its functionality has been integrated into the main AdminEventEditorPage form.
// It's kept here temporarily for reference, but can be removed once the new implementation is confirmed to work properly.
import React from 'react';

const EventRegistrationAdmin: React.FC = () => {
  return null;
};

export default EventRegistrationAdmin;
